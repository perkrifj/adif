package no.la1k.labeler;

/**
 * Enum representing useful fields from an adif-export made from 
 * N1MM Logger V14.1.0
 * 
 * Note: All of these fields are not neccecarily us used for generating a label
 *
 * @author la6xta
 */
public enum LabelFields {
	CALL,
	QSO_DATE,
	TIME_ON,
	BAND,
	STATUION_CALLSIGN,
	FREQ,
	CONTEST_ID,
	FREQ_RX,
	MODE,
	RST_RCVD,
	RST_SENT,
	OPERATOR;
}
